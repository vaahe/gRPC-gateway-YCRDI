syntax = "proto3";

option csharp_namespace = "Services.Auth";

package auth;

service AuthService {
	rpc SignIn (SignInRequest) returns (SignInResponse);
}

message SignInRequest {
	string username = 1;
	string password = 2;
}

message SignInResponse {
	string user_id = 1;
}